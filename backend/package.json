{
  "name": "focusmatrix-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-dependency backend API for the FocusMatrix prototype",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:init": "node src/scripts/init-db.js"
  },
  "dependencies": {
    "mongodb": "^6.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
